# Security Policy

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。不要在公开 Issue、讨论区或 Pull Request 中提交以下内容：

- 飞书 App Secret、access token、refresh token 或授权码；
- 真实 `config.json`；
- 客户名称、会议正文、逐字稿或内部文档链接；
- 仍然有效的带签名下载地址。

报告中请提供能够复现问题的最小信息，并用无敏感内容的测试会议替代真实业务资料。

## 凭证泄露处理

凭证一旦进入 Git 历史、公开页面、日志、截图或聊天记录，应立即在飞书开放平台撤销或重置。删除文件、编辑消息或改写 Git 历史不能保证旧凭证没有被复制。

建议同时执行：

1. 重置 App Secret 或撤销对应用户授权；
2. 重新完成飞书应用发布与个人 OAuth；
3. 检查 Git 历史、CI 日志、Release 附件和 Issue；
4. 使用 `doctor` 验证新凭证，确认旧凭证已经失效。

## 本地数据

Skill 将飞书应用配置和个人 OAuth 令牌保存在 Skill 根目录的 `config.json`。该文件已被 Git 忽略，但仍属于敏感文件；不要提交它，也不要把已经配置过的 Skill 目录复制给其他人。重新安装或替换 Skill 目录前应安全备份自己的 `config.json`。长正文、逐字稿和证据包可能写入系统临时目录，使用者应按照所在组织的终端、备份、磁盘加密和数据保留政策管理这些文件。

Skill 默认只读，不修改飞书内容、不发送消息、不自动申请权限，也不自动下载原始音视频。
